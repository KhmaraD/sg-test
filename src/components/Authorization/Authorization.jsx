import React from 'react';
import AuthForm from "./AuthForm/AuthForm";

const Authorization = () => {
    return (
        <div>
            <h1>Authorization</h1>
            <AuthForm />
        </div>
    );
};

export default Authorization;